#include "main.h"

/**
 * description main - check the code
 * print_alphabet prints the alphabet in lowercase /n
 */
void print_alphabet(void)
{
	char letter;

	for (letter = 'a'; letter <= 'z'; letter++)
		_putchar(letter);

	_putchar('\n');
}
